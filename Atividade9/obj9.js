function retangulo(base, altura) {
    this.base = base;
    this.altura = altura;            
}

var objRetangulo = new retangulo(5, 3);
objRetangulo.area = base * altura;

alert(objRetangulo.base + " * " + ObjRetangulo.altura + ' área: ' + ObjRetangulo.area);

